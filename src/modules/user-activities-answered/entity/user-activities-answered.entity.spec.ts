import { UserActivitiesAnsweredEntity } from './user-activities-answered.entity';

describe('UserActivitiesAnsweredEntity', () => {
  it('should be defined', () => {
    expect(new UserActivitiesAnsweredEntity()).toBeDefined();
  });
});
